# Port auf dem Host, unter dem das Dashboard erreichbar ist
HOST_PORT=8080

# Pfad zum Navidrome-Datenverzeichnis (muss navidrome.db enthalten)
NAVIDROME_DB_PATH=/pfad/zu/navidrome/data

# Optional: Flask Debug-Modus (nur für Entwicklung)
FLASK_DEBUG=false
